# scripts/entity-legacy — archived pre-substrate entity tooling

Superseded by the disambiguation-first substrate pipeline (see `scripts/entity-read/CLAUDE.md`).
Kept for history/reference only; do NOT run — these use retired patterns (literal name-match,
fact-JSON extraction, reactive cleanup, name-based merge). git log preserves full provenance.
